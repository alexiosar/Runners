---
import { races } from "../data/races";

// El home muestra solo las próximas 5 — el resto vive en /calendario.
const preview = races.slice(0, 5);
---

<section class="block" id="calendario">
  <div class="wrap">
    <div class="block-head reveal">
      <div>
        <span class="eyebrow">Agenda</span>
        <h2 class="display">Próximas carreras</h2>
      </div>
      <a class="see-all" href="/calendario"
        >Ver calendario completo ({races.length}) →</a
      >
    </div>
    <div class="cal-table-wrap reveal">
      <table class="cal">
        <thead>
          <tr
            ><th>Fecha</th><th>Carrera</th><th>Ciudad</th><th>Distancias</th><th
              >Tipo</th
            ></tr
          >
        </thead>
        <tbody>
          {
            preview.map((r) => (
              <tr>
                <td class="date mono">{r.date}</td>
                <td class="race-name">{r.name}</td>
                <td class="city">{r.city}</td>
                <td class="mono">{r.distances}</td>
                <td>
                  <span class="tag">{r.tag}</span>
                </td>
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </div>
</section>

